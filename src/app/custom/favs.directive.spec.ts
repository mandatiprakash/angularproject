import { FavsDirective } from './favs.directive';

describe('FavsDirective', () => {
  it('should create an instance', () => {
    const directive = new FavsDirective();
    expect(directive).toBeTruthy();
  });
});
